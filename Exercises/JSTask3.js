let a = "A"
let b = 2
let c = "Three"
let d = "4"

console.log(`If a = ${a}, then why doesn't b = B? Instead it comes back as ${b}. The same with c (${c}) and d (${d}).`)

function sayHello() {
    return 
    {
      "Someone says hello";
    }
  }
//   return has an enter after it so the ASI would try and place a ; there.